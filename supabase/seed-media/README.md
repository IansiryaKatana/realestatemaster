# Bundled CMS & property images for clean installs.
#
# Files live under `cms-media/` mirroring Supabase Storage paths.
# Regenerate from live: npm run download:media
# Upload after db push:  npm run seed:media  (needs SUPABASE_SERVICE_ROLE_KEY in .env)
